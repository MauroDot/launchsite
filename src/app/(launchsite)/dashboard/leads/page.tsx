import Link from "next/link";
import { listUserLeads } from "@/lib/leads/service";
export const dynamic = "force-dynamic";
export default async function LeadsPage() { const leads = await listUserLeads(); return <section className="mx-auto max-w-6xl px-5 py-12"><div className="flex items-end justify-between"><div><p className="eyebrow">Customer messages</p><h1 className="mt-4 text-4xl font-semibold">Leads</h1></div><Link className="text-sm font-semibold underline" href="/dashboard">Back to dashboard</Link></div>{leads.length === 0 ? <p className="mt-10 rounded-2xl border border-dashed p-10 text-center text-slate-600">Your contact form submissions will appear here.</p> : <div className="mt-8 space-y-3">{leads.map((lead) => <Link className="block rounded-2xl border p-5 hover:bg-slate-50" href={`/dashboard/leads/${lead.id}`} key={lead.id}><div className="flex flex-wrap justify-between gap-2"><div><h2 className="font-semibold">{lead.name}</h2><p className="text-sm text-slate-600">{lead.email} · {lead.project.businessName}</p></div><span className="text-sm font-semibold">{lead.status}</span></div><p className="mt-3 line-clamp-2 text-sm text-slate-700">{lead.message}</p><p className="mt-3 text-xs text-slate-500">{lead.createdAt.toLocaleString()}</p></Link>)}</div>}</section>; }
