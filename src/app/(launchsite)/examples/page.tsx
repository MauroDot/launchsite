/* eslint-disable @next/next/no-img-element -- Demo media is user-configured external media. */
import Link from "next/link";
import { listDemoProjects } from "@/lib/project-repository";
export const metadata = { title: "Examples | LaunchSite", description: "Selected websites created with LaunchSite." };
export default async function ExamplesPage() { const projects = await listDemoProjects(); return <main className="mx-auto max-w-6xl px-5 py-14 sm:px-8"><p className="eyebrow">Examples</p><h1 className="mt-4 text-4xl font-semibold">LaunchSite examples</h1>{projects.length === 0 ? <p className="mt-8 rounded-xl border border-dashed p-6 text-slate-600">No demo projects are marked yet. Mark a project as a demo to show it here.</p> : <div className="mt-10 grid gap-5 md:grid-cols-2">{projects.map((project) => <article className="overflow-hidden rounded-2xl border" key={project.id}>{project.workSamples[0]?.mediaUrl && <img alt="" className="aspect-video w-full object-cover" src={project.workSamples[0].mediaUrl} />}<div className="p-6"><p className="text-sm text-slate-500">{project.business.category} · {project.siteSettings?.layoutFamily} · {project.visualStyle}</p><h2 className="mt-2 text-xl font-semibold">{project.business.businessName}</h2><p className="mt-2 text-slate-600">{project.business.description}</p><Link className="mt-5 inline-block font-semibold" href={`/examples/${project.slug}`}>View Example</Link></div></article>)}</div>}</main>; }
