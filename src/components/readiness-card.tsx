import Link from "next/link";
import type { PersistedWebsiteProject } from "@/lib/website-types";
import { getProjectReadiness } from "@/lib/readiness";
export function ReadinessCard({ project }: { project: PersistedWebsiteProject }) { const items = getProjectReadiness(project); const complete = items.filter((item) => item.complete).length; return <section className="mx-auto mt-6 max-w-6xl rounded-2xl border border-slate-200 p-6" id="readiness"><div className="flex flex-wrap items-end justify-between gap-2"><div><h2 className="text-xl font-semibold">Site readiness</h2><p className="mt-1 text-sm text-slate-600">{complete} of {items.length} recommended steps complete. Custom domains are optional.</p></div><span aria-label={`${complete} of ${items.length} complete`} className="text-sm font-semibold">{Math.round(complete / items.length * 100)}%</span></div><div className="mt-4 h-2 overflow-hidden rounded-full bg-slate-100"><div className="h-full rounded-full bg-lime-500" style={{ width: `${complete / items.length * 100}%` }} /></div><ul className="mt-5 grid gap-3 sm:grid-cols-2">{items.map((item) => <li className="flex items-center gap-2 text-sm" key={item.key}><span aria-hidden className={item.complete ? "text-lime-700" : "text-slate-400"}>{item.complete ? "✓" : "○"}</span>{item.href ? <Link className="underline decoration-slate-300 underline-offset-2" href={item.href}>{item.label}</Link> : <span>{item.label}</span>}</li>)}</ul></section>; }
