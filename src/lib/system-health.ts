import "server-only";
import { prisma } from "@/lib/prisma";
import { configStatus } from "@/lib/operational-logging";
export async function getSystemHealth() { let database: "HEALTHY" | "DEGRADED" = "HEALTHY"; try { await prisma.$queryRaw`SELECT 1`; } catch { database = "DEGRADED"; } return { application: "HEALTHY", database, authentication: configStatus(process.env.AUTH_SECRET) === "CONFIGURED" && configStatus(process.env.AUTH_GOOGLE_ID) === "CONFIGURED" && configStatus(process.env.AUTH_GOOGLE_SECRET) === "CONFIGURED" ? "CONFIGURED" : "MISSING", billing: [process.env.STRIPE_SECRET_KEY, process.env.STRIPE_WEBHOOK_SECRET, process.env.STRIPE_STARTER_PRICE_ID, process.env.STRIPE_BUSINESS_PRICE_ID].every(Boolean) ? "CONFIGURED" : "MISSING", email: [process.env.RESEND_API_KEY, process.env.LEAD_NOTIFICATION_FROM].every(Boolean) ? "CONFIGURED" : "MISSING", media: [process.env.NEXT_PUBLIC_CLOUDINARY_CLOUD_NAME, process.env.CLOUDINARY_API_KEY, process.env.CLOUDINARY_API_SECRET].every(Boolean) ? "CONFIGURED" : "MISSING", ai: configStatus(process.env.OPENAI_API_KEY), domains: [process.env.VERCEL_API_TOKEN, process.env.VERCEL_PROJECT_ID].every(Boolean) ? "CONFIGURED" : "MISSING", canonicalUrl: configStatus(process.env.NEXT_PUBLIC_APP_URL), rateLimiter: "IN-MEMORY / INSTANCE-LOCAL", environment: process.env.NODE_ENV === "production" ? "PRODUCTION" : "DEVELOPMENT" } as const; }
